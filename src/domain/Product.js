export class Product {

    constructor(name, productType, bad, good) {
        this.name = name;
        this.badIngredients = bad;
        this.goodIngredients = good;
        this.productType = productType;
    }
}